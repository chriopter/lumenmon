#!/bin/bash
# Refreshes the Rails dependency lockfile for the console app.
# Run this before dependency PRs so Docker builds stay reproducible.

set -e

cd "$(dirname "$0")/.."

echo "Updating console Rails dependencies..."

(cd console && bundle update)

echo "Done! Dependencies updated successfully."
